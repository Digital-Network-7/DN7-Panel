// =========================================================================
// Self-update — version modal (manual / auto), source probing, progress.
// Triggered from the sidebar version line. Talks to /api/update/*.
// =========================================================================
// (populated below)
